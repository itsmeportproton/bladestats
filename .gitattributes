# Проект собирается и на Linux, поэтому в репозитории всегда LF независимо от того,
# с какой машины сделан коммит. Шелл-скрипты и манифесты Vulkan-слоя на CRLF ломаются.
* text=auto eol=lf

*.ttf binary
