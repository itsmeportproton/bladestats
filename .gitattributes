# The project also builds on Linux, so the repository always stores LF regardless of which
# machine a commit came from. Shell scripts and Vulkan layer manifests break on CRLF.
* text=auto eol=lf

*.ttf binary
