/target
**/*.rs.bk
*.pdb

# The font is fetched by hand rather than stored here (see assets/fonts/README.md).
/assets/fonts/*.ttf

# Local config used for manual runs
/bladestats.toml

# The HTML mockup the native settings window replaced, and the editor's launch config for it.
# Both are development leftovers rather than anything a reader needs.
/design
/.claude
